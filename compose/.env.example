# Базовый URL для доступа к сервисам (например: http://192.168.1.100 или http://homelab.local)
BASE_URL=http://localhost

# Vaultwarden (Password Manager)
# Сгенерируйте безопасный токен: openssl rand -base64 48
VAULTWARDEN_ADMIN_TOKEN=CHANGE_THIS_TO_SECURE_RANDOM_TOKEN
# Разрешить регистрацию новых пользователей (после создания администратора рекомендуется отключить)
VAULTWARDEN_SIGNUPS_ALLOWED=false

# Filebrowser
FILEBROWSER_USERNAME=admin
FILEBROWSER_PASSWORD=CHANGE_THIS_TO_SECURE_PASSWORD

# Restic Backup
# Локальный репозиторий (внешний диск). Оставьте пустым, если не используете.
RESTIC_REPO_LOCAL=/mnt/backup/restic
# Облачный репозиторий (Яндекс Диск). Оставьте пустым, если не используете.
RESTIC_REPO_CLOUD=/mnt/yandex-disk/homelab-backups
# Пароль для шифрования бэкапов (сгенерируйте надежный пароль!)
RESTIC_PASSWORD=CHANGE_THIS_TO_SECURE_PASSWORD

# Additional Vaultwarden Settings (optional)
# VAULTWARDEN_DISABLE_2FA_REMEMBER=true
# VAULTWARDEN_ICON_DOWNLOAD_TIMEOUT=10

# Additional Backup Settings
# What to backup (space-separated paths)
BACKUP_TARGETS=/srv/data/vaultwarden /srv/homelab/homelab-server

# Restic retention policy
# --keep-daily 7 --keep-weekly 4 --keep-monthly 6 --keep-yearly 1
RESTIC_RETENTION=--keep-daily 7 --keep-weekly 4 --keep-monthly 6
