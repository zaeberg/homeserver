  🚨 Критические проблемы

  1. Отсутствие HTTPS/shielding для критических сервисов

  Проблема:
  - Caddy слушает только порт 80 (HTTP)
  - Все сервисы доступны без шифрования
  - Vaultwarden (менеджер паролей!) передаёт данные в открытом виде

  Файлы:
  - compose/compose.yml:7 — только порт 80
  - caddy/Caddyfile:1 — нет TLS конфигурации

  Риск: Перехват паролей, токенов, файлов в локальной сети

  Решение:
  # compose/compose.yml
  ports:
    - "80:80"
    - "443:443"  # Добавить HTTPS

  # Caddyfile - добавить автоматический HTTPS
  {$BASE_URL} {
      # Caddy автоматически получит Let's Encrypt сертификат
      # или можно использовать self-signed для локальной сети
  }

  ---
  2. Filebrowser монтирует весь /srv/data без ограничений

  Проблема:
  # compose/compose.yml:69
  volumes:
    - /srv/data/filebrowser:/srv
    - /srv/data:/data  # ← ПРОБЛЕМА: весь /srv/data доступен!

  Через Filebrowser можно получить доступ к:
  - Базе данных Vaultwarden
  - Конфигам Syncthing
  - Логам с возможными секретами

  Риск: Компрометация всех данных через один сервис

  Решение:
  volumes:
    - /srv/data/filebrowser:/srv
    - /srv/data/shared:/data  # Только отдельная папка для шаринга

  ---
  3. .env файл не бэкапится автоматически

  Проблема:
  - backup.sh:109 исключает .env из бэкапа
  - При восстановлении нужно вручную восстанавливать секреты
  - Нет инструкций по хранению .env в безопасном месте

  Риск: Полная потеря доступа при восстановлении из бэкапа

  Решение:
  1. Создать compose/.env.template без секретов
  2. Добавить инструкцию по хранению секретов в password manager
  3. Опционально: зашифрованный бэкап .env с помощью GPG

  ---
  4. Отсутствует мониторинг бэкапов

  Проблема:
  - Systemd timer'ы не отправляют уведомления
  - Нет возможности узнать о failed бэкапе
  - backup.sh пишет лог только в файл

  Файлы:
  - systemd/homelab-backup-*.timer — нет OnFailure=

  Риск: Неworking бэкапы обнаружатся только при восстановлении

  Решение:
  # Добавить в systemd unit
  [Service]
  # ... existing config ...
  OnFailure=notify-backup-failed@%n.service

  # Или интегрировать с Uptime Kuma:
  ExecStartPost=/usr/bin/curl -X POST http://uptime-kuma:3001/api/push/HOOK_ID?status=up

  ---
  ⚠️ Серьёзные проблемы

  5. Отсутствие resource limits для контейнеров

  Проблема:
  Все контейнеры могут использовать неограниченные ресурсы

  Файл: compose/compose.yml

  Риск: Один контейнер может уронить весь сервер (OOM)

  Решение:
  services:
    vaultwarden:
      deploy:
        resources:
          limits:
            cpus: '0.5'
            memory: 512M
          reservations:
            cpus: '0.25'
            memory: 256M

  ---
  6. Отсутствует автоматизация обновления контейнеров

  Проблема:
  - Обновление только ручное (operations.md:14)
  - Нет security scanning
  - Устаревшие образы с уязвимостями

  Риск: Запуск уязвимых версий

  Решение:
  - Добавить watchtower или ouroboros для автообновления
  - Или добавить monthly maintenance в systemd timer
  - Добавить trivy для сканирования образов

  ---
  7. Неконсистентная обработка ошибок в скриптах

  Проблема:
  # validate.sh:65
  if [ ! -f "compose/.env" ]; then
      if [ -f "compose/.env.example" ]; then
          cp compose/.env.example compose/.env.temp
          mv compose/.env.temp compose/.env  # ← Race condition!

  Файл: scripts/validate.sh:68

  Риск: Потеря существующего .env при параллельном запуске

  Решение: Использовать mktemp или атомарные операции

  ---
  8. restore_test.sh требует jq без проверки

  Проблема:
  # restore_test.sh:95
  LATEST_SNAPSHOT=$(restic list snapshots --json | jq -r '...')

  Нет проверки, что jq установлен

  Файл: scripts/restore_test.sh:95

  Риск: Криптические ошибки при отсутствии jq

  Решение: Добавить проверку в начале скрипта

  ---
  9. Отсутствует проверка монтирования бэкап-диска

  Проблема:
  backup.sh не проверяет, смонтирован ли backup disk перед запуском

  Файл: scripts/backup.sh

  Риск: Бэкап на корень диска вместо внешнего диска

  Решение:
  # Добавить в backup.sh
  if ! mountpoint -q "$RESTIC_REPO"; then
      error "Backup repository not mounted!"
      exit 1
  fi

  ---
  🔧 Средние проблемы

  10. Hardcoded пути в скриптах

  Проблема:
  # deploy.sh:40
  docker compose --env-file compose/.env -f compose/compose.yml down

  Множественное повторение путей

  Решение: Вынести в переменные

  ---
  11. Отсутствие network policies

  Проблема:
  - Все сервисы в одной сети internal
  - Нет ограничений communication между сервисами

  Файл: compose/compose.yml:105-110

  Риск: Компрометация одного сервиса даёт доступ ко всем

  Решение: Разделить сети для разных групп сервисов

  ---
  12. Caddy хранит данные в /srv/data/caddy

  Проблема:
  Caddy может хранить TLS сертификаты, но для HTTP-only они не нужны

  Решение: Либо убрать, либо добавить HTTPS

  ---
  13. Отсутствует проверка свободного места

  Проблема:
  Нигде не проверяется свободное место на диске перед бэкапом

  Риск: Failed бэкапы при заполненном диске

  Решение: Добавить проверку в backup.sh

  ---
  14. Syncthing без auth по умолчанию

  Проблема:
  Syncthing доступен без авторизации через reverse proxy

  Файл: compose/compose.yml:41-59

  Риск: Несанкционированный доступ к файлам

  Решение: Добавить basic auth в Caddy или настроить в Syncthing

  ---
  15. Отсутствует rate limiting в Caddy

  Проблема:
  Нет защиты от brute-force

  Файл: caddy/Caddyfile

  Риск: Подбор паролей

  Решение:
  {
      # Добавить глобальные директивы
      rate_limit {
          zone dynamic {
              key {remote_host}
              events 100
              window 1m
          }
      }
  }

  ---
  📝 Мелкие проблемы и улучшения

  16. Неполный .gitignore

  Файл: .gitignore

  Отсутствует:
  - compose/.env.local
  - compose/.env.production
  - *.bak

  ---
  17. Отсутствие CI/CD валидации

  Нет проверки PR'ов на наличие секретов

  Решение: Добавить pre-commit hook или GitHub Actions

  ---
  18. healthcheck.sh использует BASE_URL без проверки

  Файл: scripts/healthcheck.sh:64

  Если BASE_URL не задан, используется localhost (может не работать)

  ---
  19. Отсутствие версионности в документации

  Нет указания версий сервисов в документации

  Пример: В docs/01_repo_overview.md указаны сервисы без версий

  ---
  20. Неконсистентное именование

  - homelab-backup-local vs homelab-restore-test (без суффикса)

  ---
  21. Отсутствует проверка зависимостей в скриптах

  Нет проверок наличия docker, restic, curl, jq

  ---
  22. Caddy healthcheck невалиден

  Файл: compose/compose.yml:15-19
  healthcheck:
    test: ["CMD", "caddy", "validate", "--config", "/etc/caddy/Caddyfile"]

  Это проверяет конфиг, а не то, что Caddy работает

  Решение:
  test: ["CMD", "curl", "-f", "http://localhost:80/"]

  ---
  23. Отсутствует graceful shutdown

  Нет timeout и stop_grace_period в compose

  ---
  24. Отсутствует автоматизированное тестирование restore

  restore_test.sh нужно запускать вручную

  Решение: Добавить в systemd monthly timer

  ---
  25. Невозможность работы без интернета

  Яндекс Диск требует постоянного подключения
